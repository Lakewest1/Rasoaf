# ============================================
# RASOAF TRAVELS & TOURS - REDIRECTS
# ============================================

# JavaScript files
/*.js /:splat.js 200
/*.css /:splat.css 200

# Image files
/*.png /:splat.png 200
/*.jpg /:splat.jpg 200
/*.jpeg /:splat.jpeg 200
/*.svg /:splat.svg 200
/*.webp /:splat.webp 200
/*.ico /:splat.ico 200

# JSON files
/*.json /:splat.json 200

# Assets folder
/assets/* /assets/:splat 200

# Textures folder - IMPORTANT for 3D Earth!
/textures/* /textures/:splat 200

# SPA fallback (ONLY if no file matches)
/* /index.html 200