# ============================================
# STATIC ASSETS - LET THEM LOAD FIRST
# ============================================

# JavaScript files
/*.js /:splat.js 200
/*.css /:splat.css 200

# Image files
/*.png /:splat.png 200
/*.jpg /:splat.jpg 200
/*.jpeg /:splat.jpeg 200
/*.svg /:splat.svg 200
/*.webp /:splat.webp 200
/*.ico /:splat.ico 200

# JSON files
/*.json /:splat.json 200

# ============================================
# ASSETS FOLDER - LET THEM LOAD
# ============================================

/assets/* /assets/:splat 200

# ============================================
# TEXTURES FOLDER - LET THEM LOAD
# ============================================

/textures/* /textures/:splat 200

# ============================================
# SPA FALLBACK - ONLY IF NO FILE MATCHES
# ============================================

/* /index.html 200